# 2026-05-04 Daily Log

## Hermes Dashboard 搭建完成

- **模型切换**: deepseek-chat-v3-0324 → deepseek-v4-flash (OpenRouter, 1M context)
- **context_length 配置**: 主模型和 auxiliary.compression 都设置了 65536 override，解决 context window < 64K 错误
- **Python 依赖**: 通过 uv pip install 安装了 fastapi + uvicorn（清华镜像源）
- **Web 前端构建**: Windows Node.js v25 太新导致 Vite 静默崩溃，需在 WSL 原生文件系统构建
  - 构建流程: tar 复制 web/ → ~/.hermes-build/web → npm install + npm run build → tar 复制 web_dist 回 Windows
  - WSL 系统 Node.js v20.20.2 可正常构建
  - 构建时间约 2-3 分钟
- **Dashboard 启动**: `hermes dashboard --host 0.0.0.0 --no-open`，监听 9119 端口
  - Windows localhost 已可访问 http://127.0.0.1:9119
  - 局域网访问需管理员执行防火墙放行: `New-NetFirewallRule -DisplayName 'Hermes Dashboard 9119' -Direction Inbound -LocalPort 9119 -Protocol TCP -Action Allow`
- **WSL uv 已安装**: v0.11.8 at ~/.local/bin/uv（用于 pip install，清华镜像 -i https://pypi.tuna.tsinghua.edu.cn/simple）
