# <技能名> Skill 交接说明（给 <目标 AI 名>）

> 本文档供 <目标 AI，如 WordBuddy/Codex> 读取，以便它也能生成/完善同一个技能。
> 生成方：Hermes Agent（<技能名> 的现有维护者）

━━━━━━━━━━━━━━━━━━━━━━
一、这是什么
━━━━━━━━━━━━━━━━━━━━━━
- 一句话定位：<技能服务什么目标>
- 服务对象：<机构/用户/行业>
- 使用场景：<何时被调用>

━━━━━━━━━━━━━━━━━━━━━━
二、技能的位置与结构
━━━━━━━━━━━━━━━━━━━━━━
主目录（Obsidian vault，Hermes 通过软链 ~/.hermes/skills/<名> 指向它）：
  /mnt/d/obsidian-vault/50-Skills/<技能名>/

结构：
- SKILL.md —— 主技能文件（工作流程、红线、输出格式）
- <启动提示词>.md —— 给 AI 的角色设定/读取顺序/底线规则（若存在）
- references/ —— <每个文件一句话说明>
- templates/ —— <每个模板一句话说明>
- cases/ —— <案例库命名格式>

━━━━━━━━━━━━━━━━━━━━━━
三、核心方法论（<目标 AI> 要理解的灵魂）
━━━━━━━━━━━━━━━━━━━━━━
1. <总纲/第一原则>
2. <核心概念 A —— 定义 + 为什么关键>
3. <核心概念 B —— 定义 + 两条分支路径>
4. <流程顺序：先 vs 后，不能跳步>
5. <红线：不可逾越的事>

━━━━━━━━━━━━━━━━━━━━━━
四、当前版本与最近更新（<版本号>，<日期>）
━━━━━━━━━━━━━━━━━━━━━━
- <上一版做了什么>
- <本版新加了什么，列出每条改动>
- 整合的外部资料来源（书目/文档 + 年份 + 吸收深度）

━━━━━━━━━━━━━━━━━━━━━━
五、给 <目标 AI> 的建议（请交接给 <目标 AI> 输出）
━━━━━━━━━━━━━━━━━━━━━━
1. 若你要"生成或完善"同一个技能，以本说明 + 主目录现有文件为准，保持结构一致（文件名、编号、命名格式）。
2. 优先补充的方向（尚未完成的高频案由/章节/模板）：
   - <方向一>
   - <方向二>
3. 每个新条目按 <标准结构> 写（<逐项列出>），<重点优先项> 优先。
4. 红线纪律必须同步：<列举该技能红线>。
5. 案例积累：办完一个按 <模板名> 存脱敏记录，重点填"可复用要点"与"踩过的坑"。
6. 从其它资料补充时：引用政策前务必核对现行有效版本并标注官方文号；时间敏感内容（税率/标准/时限）只吸收方法思路，不收录可能过期的数字。

━━━━━━━━━━━━━━━━━━━━━━
六、如何让调用方自动触发（供 <目标 AI> 知悉）
━━━━━━━━━━━━━━━━━━━━━━
SKILL.md frontmatter 已含 triggers：<列出触发词，注明单字词误触发风险>

（企业内部工具/企微端调用属部署范畴，需按环境另行配置，不在本文件范围内。）
