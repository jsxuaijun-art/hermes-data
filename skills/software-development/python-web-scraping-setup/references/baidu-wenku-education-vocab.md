# 百度文库教育词汇资源爬取

## 2026-07-04 实测：苏州中考英语词汇搜索

### 目标
搜索「苏州中考英语词汇表」的完整 word list，用于生成默写本。

### 使用工具
Playwright（真实浏览器）+ 反检测初始化脚本

### 搜索结果

| 工具 | 结果 |
|:--|:--|
| Bing CN (cn.bing.com) | ✅ 正常返回搜索结果，但多为中考资讯网站，无直接词汇文件下载 |
| 百度搜索 | ❌ 触发滑块验证码，无法自动化访问 |
| 百度文库搜索 | ✅ 正常加载，可搜到目标文档，预览片段可提取词汇 |
| 百度文库文档页 | ⚠️ 部分文档可看标题/预览，完整内容需登录 + 付费 |

### 百度文库搜索页访问模式

**搜索页访问**：
```python
from playwright.sync_api import sync_playwright

with sync_playwright() as p:
    browser = p.chromium.launch(headless=True)
    context = browser.new_context(
        viewport={'width': 1920, 'height': 1080},
        user_agent='Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/124.0.0.0 Safari/537.36',
        locale='zh-CN',
    )
    page = context.new_page()
    # 反检测脚本
    page.add_init_script("""
        Object.defineProperty(navigator, 'webdriver', { get: () => undefined });
        Object.defineProperty(navigator, 'plugins', { get: () => [1,2,3,4,5] });
        window.chrome = { runtime: {} };
    """)
    
    page.goto('https://wenku.baidu.com/search?word=苏州中考词汇表&org=0', 
              wait_until='domcontentloaded', timeout=30000)
    page.wait_for_timeout(5000)
    
    # 提取页面文本（搜索结果预览包含词汇片段）
    text = page.inner_text('body')
```

**搜索结果预览提取**：
百度文库搜索结果的body text中包含文档预览片段（如前几十个词汇），可通过正则提取：
```python
import re
words = re.findall(r'([a-z]+)\s+(n\.|v\.|adj\.|adv\.)', text, re.IGNORECASE)
```

**文档URL格式**：
```
https://wenku.baidu.com/view/<32位hex>.html?fr=search
```

### 局限性
1. **百度文库文档页需要登录**才能查看完整内容，自动化无法绕过
2. **文档预览仅显示前几条词汇**，不完整
3. **下载需要文库VIP或付费**，无法通过爬虫免费获取
4. **建议方案**：手动在百度文库搜索 → 付费下载 → 提供TXT文件给agent处理

---

## 2026-07-06 更新：IELTS词汇搜索策略

### 背景
用户想下载「正宗的新东方雅思词汇表」，试了搜狗/Bing/GitHub/知乎等多个渠道均失败。最终通过百度文库找到完整版。

### 搜索链实测结果

| 层级 | 工具 | 目标 | 结果 |
|:--:|:--|:--|:--:|
| 1 | curl_cffi | Bing.cn 搜索 | ❌ JS动态渲染拿不到结果 |
| 2 | curl_cffi | Bing.en 搜索 | ❌ 强制跳回国内版 |
| 3 | curl_cffi | 搜狗搜索（第一次） | ✅ 拿到链接（搜狗跳转格式，无法直接访问） |
| 4 | curl_cffi | 搜狗搜索（第二次） | ❌ 触发anti-spider验证码 |
| 5 | Playwright | 搜狗搜索 | ❌ 直接anti-spider拦截 |
| 6 | Browser | GitHub raw KelyBing IELTS 词典 | ❌ 429限流 + 404 |
| 7 | Browser | 国外IELTS备考站 | ❌ 超时（GFW阻断） |
| 8 | Browser | 知乎「雅思词汇」文章 | ❌ 有词汇片段但不完整，无下载链接 |
| 9 | **Browser (浏览器)** | **百度文库搜索** | ✅ **全文结果可用，评分/页数/阅读量均可见** |

### 关键词策略

搜索 IELTS 词汇表时，关键词选择直接影响结果质量：

| 关键词组合 | 推荐度 | 结果质量 |
|:--|:--:|:--:|
| 「新东方雅思词汇 词根 联想 记忆法」 | ★★★★★ | 15篇关联文档，含绿宝书系列 |
| 「雅思词汇8000 新东方在线」 | ★★★★ | 32页长文档，评分4.2，阅读1.2W |
| 「(完整版)雅思必考单词表(全)」 | ★★★ | 6页短文档，评分2.7，不完整 |
| 「雅思常用词汇及短语 完整版」 | ★★ | 2页，短语为主 |
| 「雅思词汇表 完整版」（泛搜） | ★★ | 结果泛，多为预览版/截取版 |

### 已确认的完整文档列表

通过百度文库搜索页（[wenku.baidu.com](https://wenku.baidu.com)）筛选「txt」格式找到：

| 文档名 | 评分 | 页数 | 阅读量/下载量 |
|:--|:--:|:--:|:--:|
| 雅思词汇词根_联想记忆法 2463全网唯一完整txt版 | ⭐4.1 | 25页 | 3629阅读 / 137下载 |
| 雅思词汇8000（新东方在线） | ⭐4.2 | 32页 | 1.2W阅读 |
| (完整版)雅思必考单词表(全) | ⭐2.7 | 6页 | 502阅读 |
| (完整版)雅思常用词汇及短语 | ⭐2.8 | 2页 | 34阅读 |
| 新东方绿宝书·雅思词汇词根+联想记忆法(乱序版)说明书 | ⭐4.5 | — | 228下载 / 2490阅读 |
| 俞敏洪_雅思词汇词根联想记忆法 | ⭐4.1 | — | 193下载 / 1836阅读 |

**关联文档集（同一套搜索返回的15篇）**：还包括俞敏洪版、乱序版、便携版等多个变体。

### 最佳操作流程（用户有百度文库VIP时）

1. 打开 `https://wenku.baidu.com/search?word=新东方雅思词汇词根联想记忆法&org=0`
2. 点击「txt」格式筛选按钮 → 只看纯文本文档
3. 查看评分 >4.0 且阅读 >1000 的文档 → 新东方绿宝书系列即为完整版
4. 用户用VIP登录后付费/免费下载TXT文件到桌面
5. 提供文件路径给agent → agent用 `build_root.py` 改词源即可输出IELTS版默写本

### 经验总结
- 百度文库搜索页无需登录即可访问，搜索结果包含标题/评分/页数/阅读量/预览片段
- 评分 <4.0 或页数少（<10页）的词汇文档通常是截取版
- 真正完整的IELTS词表只有新东方系列（俞敏洪编著绿宝书 + 新东方在线配套词表）
- 用户曾反馈「百度文库的雅思词汇表不完整」→ 原因是搜了泛关键词，应该精准搜「新东方」+「词根」组合
- **自动化的可行度**：搜索页可自动化查看（Playwright无头可通过），但下载必须人工（登录+VIP）

---

### 已知可用的苏州中考相关文档
| 文档名 | 链接片段 |
|--------|----------|
| 2024中考复习必背初中英语单词词汇表(苏教译林版) | `view/fe165c1b07a1b0717fd5360cba1aa81145318f8e.html` |
| 江苏省中考英语词汇整理(顺序版) | `view/aece7165900ef12d2af90242a8956bec0975a5b6.html` |
| 苏州中考词汇表（59页） | 搜索「苏州中考词汇表」第一个结果 |

### 经验总结（综合版）
- Playwright + 反检测脚本 ✅ 可以访问百度文库搜索页
- 但完整文档获取需要用户手动操作（付费下载）
- 爬虫能做的是：搜索定位、预览提取、给出精准链接
- 完整词库获取 → 推荐用户从百度文库付费下载后提供
- **找词汇表的关键词技巧**：搜「出版社名+书名+类型」比搜「词汇表 完整版」有效得多
